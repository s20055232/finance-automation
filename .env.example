# .env.example — docker-compose 變數插值（複製為 .env 並填入真實值）
#
# 這個檔案的變數由 docker-compose.yml 讀取（非 backend/.env）。
# backend 專用設定請看 backend/.env.example。

# Cloudflare Tunnel 網域
PUBLIC_URL=

# Kratos session 加密 key（各 32 字元，可用 make gen-secrets 產生）
KRATOS_SECRET_COOKIE=
KRATOS_SECRET_CIPHER=
